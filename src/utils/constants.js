export const drawerWidth = 240;

export const notificationColors = Object.freeze({
  // GREEN
  success: { color: '#4B8054', textColor: '#fff' },
  error: { color: '#804B4B', textColor: '#fff' },
  warning: { color: '#806E4B', textColor: '#fff' },
  update: { color: '#4B807F', textColor: '#fff' },
  default: { color: '#545454', textColor: '#fff' },
});

export const scheduleColors = Object.freeze({
  // GREEN
  active: { color: '#4B8054', textColor: '#fff' },
  delayed: { color: '#804B4B', textColor: '#fff' },
  inactive: { color: '#545454', textColor: '#fff' },
});
